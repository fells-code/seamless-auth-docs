export const meta = { title: "Introduction", order: 1, tags: ["overview"] };

# SeamlessAuth

SeamlessAuth is a passwordless auth platform with **single-tenant** or **hybrid** deployment options.

- No redirects or provider lock-in
- Drop-in React components
- Secure cookies, JWKS, WebAuthn
- AWS-native provisioning via Terraform/ECS

## What you’ll build

- Install the SDK
- Configure your app
- Authenticate users

## Who is this for?

Developers who want production-grade auth without wrestling with legacy flows.
