export const meta = { title: "React SDK", order: 3, tags: ["react"] };

# React SDK

## Provider API

- `user`, `logout`, `deleteUser`, `isAuthenticated`, `hasRole`, `apiHost`, `token`

## Example

```tsx
<AuthProvider apiHost="https://api.example.com" token="...">
  ...
</AuthProvider>
```
